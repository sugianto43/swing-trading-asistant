# Phase 1 — Technical Design Document

## Architecture
Establish monorepo/application boundaries, configuration, migrations, API conventions, testing, Docker Compose, and CI.

## Required Interfaces
Health/version endpoints; configuration loader; DB session; migration runner.

## Data
Initial user/auth-ready schema without coupling to a specific auth vendor.

## Tests
Configuration, migrations, DB connection, health endpoint, frontend build.
